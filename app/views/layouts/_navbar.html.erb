<div class="navbar-div">
  <nav class="navbar navbar-expand-lg navbar-background">
    <div class="container-fluid">
      <strong><%= link_to "EditorNet", root_path, class: "my-navbar-brand txt-white margin-left" %></strong>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="my-navbar-nav me-auto mb-2 mb-lg-0">
          <% if user_signed_in? %>
            <li class="nav-item txt-white">
              <%= link_to "New Post", new_post_path, class: "nav-link" %>
            </li>
          <% end %>
          <li class="nav-item txt-white">
            <%= link_to "Home", home_path, class: "nav-link" %>
          </li>
          <li class="nav-item txt-white">
            <%= link_to "Find Editors", index_path, class: "nav-link" %>
          </li>
          <% if user_signed_in? %>
            <li class="nav-item d-flex align-items-center txt-white">
              <span class="nav-item txt-white">Hi, <%= current_user.username || current_user.email %></span>
            </li>
            <li class="nav-item d-flex align-items-center txt-white">
              <%= button_to "Sign out",
                destroy_user_session_path,
                method: :delete,
                form: { class: "d-inline m-0 p-0" },
                class: "nav-link btn btn-link p-0" %>
            </li>
          <% else %>
            <li class="nav-item"><%= link_to "Sign up", new_user_registration_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "Sign in", new_user_session_path,       class: "nav-link" %></li>
          <% end %>
          <li class="nav-item dropdown txt-white">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Dropdown
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="#">Action</a></li>
              <li><a class="dropdown-item" href="#">Another action</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="#">Something else here</a></li>
            </ul>
          </li>
        </ul>
        <form class="d-flex" role="search">
          <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"/>
          <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
      </div>
    </div>
  </nav>
</div>
